# Development and test dependencies — NOT installed in production Docker image.
# Install with: pip install -r requirements-dev.txt
-r requirements.txt
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2
pip-audit==2.7.2
